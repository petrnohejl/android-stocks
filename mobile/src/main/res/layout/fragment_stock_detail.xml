<?xml version="1.0" encoding="utf-8"?>
<layout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	xmlns:app="http://schemas.android.com/apk/res-auto">

	<data>
		<variable name="view" type="com.example.ui.StockDetailView" />
		<variable name="viewModel" type="com.example.viewmodel.StockDetailViewModel" />
	</data>

	<com.example.view.StatefulLayout
		android:id="@+id/stock_detail_stateful_layout"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:animateLayoutChanges="true"
		app:progressLayout="@layout/placeholder_progress"
		app:offlineLayout="@layout/placeholder_offline"
		app:emptyLayout="@layout/placeholder_empty"
		app:state="@{viewModel.state}">

		<ScrollView
			android:id="@+id/container_content"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:fillViewport="true"
			android:background="@color/global_bg_front">

			<LinearLayout
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:padding="@dimen/global_keyline_16"
				android:orientation="vertical">

				<TextView
					android:id="@+id/stock_detail_name"
					android:layout_width="wrap_content"
					android:layout_height="wrap_content"
					android:textAppearance="@style/TextAppearance.Stocks.Body1"
					android:text="@{viewModel.stock.name}"
					android:onClick="@{view.onClick}"
					tools:text="Hello world" />

			</LinearLayout>
		</ScrollView>
	</com.example.view.StatefulLayout>

</layout>
